FROM alpine:3.20

RUN apk add --no-cache curl jq && \
    adduser -D -u 1000 cliuser

WORKDIR /app

COPY tools/cli/pgquerynarrative-cli.sh /usr/local/bin/pgquerynarrative
RUN chmod +x /usr/local/bin/pgquerynarrative

USER cliuser:cliuser

CMD ["/bin/sh"]
